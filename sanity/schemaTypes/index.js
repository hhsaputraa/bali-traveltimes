import tour from "./tour";

/**
 * Registry of schemas for Sanity Studio.
 * Add new content type schemas here to register them with the studio interface.
 */
export const schema = {
  types: [tour],
};
